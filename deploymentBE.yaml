apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: nugrohop2003/backend:1.1
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 5000
        env:
          - name: BASE_URL_PORT
            valueFrom:
              configMapKeyRef:
                name: configmap-backend 
                key: BASE_URL_PORT
                env: 
          - name: DATABASE_USERNAME
            valueFrom:
              configMapKeyRef:
                name: configmap-backend 
                key: DATABASE_USERNAME
          - name: DATABASE_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: configmap-backend 
                key: DATABASE_PASSWORD
          - name: DATABASE_DATABASE
            valueFrom:
              configMapKeyRef:
                name: configmap-backend 
                key: DATABASE_DATABASE
          - name: DATABASE_HOST
            valueFrom:
              configMapKeyRef:
                name: configmap-backend 
                key: DATABASE_HOST
          - name: DATABASE_PORT
            valueFrom:
              configMapKeyRef:
                name: configmap-backend 
                key: DATABASE_PORT

---
apiVersion: v1
kind: Service
metadata:
  name: service-backend
spec:
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 5000

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap-backend
  labels:
    app: backend
data:
  BASE_URL_PORT: "5000"
  DATABASE_USERNAME: admin
  DATABASE_PASSWORD: "12345678"
  DATABASE_DATABASE: people
  DATABASE_HOST: database-cilist.c5obzm6tolho.ap-southeast-1.rds.amazonaws.com
  DATABASE_PORT: "3306"

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: be-ingress
  labels:
    name: be-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: backend.zonadesain.id
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: service-backend
            port: 
              number: 5000


